<div class="form-group {{ $col }}">
    <button type="{{ $type }}" class="btn btn-block btn-lg font-weight-bold text-uppercase {{ $btnClass }}">
        <i class="{{ $icon }}"></i>&nbsp;{{ $btnText }}
    </button>
</div>
